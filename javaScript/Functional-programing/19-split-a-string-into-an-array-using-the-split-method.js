function splitify(str) {
    // Only change code below this line
  const splited = str.split(/[^0-9a-zA-Z_]/);
  return splited
    // Only change code above this line
  }
  console.log(splitify("Hello World,I-am code"))